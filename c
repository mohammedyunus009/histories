  sudo apt-get update
  sudo apt-get dist-update
  sudo apt-get dist-upgrade
  sudo apt upgrade python
  sudo apt upgrade python2.7
  sudo apt upgrade python3.5
  sudo apt install pip
  sudo apt install python-pip
  sudo apt install mysql
  sudo apt-get install mysql-server
  sudo apt update vi
  sudo apt upgrade vi
  sudo apt upgrade vim
  sudo apt upgrade vim
  sudo apt install gparted
  sudo apt install geany
  sudo apt install default-jdk
  sudo apt install input_calibrator
  sudo apt install xinput_calibrator
  sudo apt install xinput-calibrator
  sudo apt install chrome
  sudo apt install googlechrome
  sudo apt install google-chrome
  sudo apt install chromium
  sudo apt install chromium-bsu
  sudo apt upgrade
echo " done1"
  sudo apt update g++
  sudo apt upgrade g++
  sudo apt upgrade gcc
  sudo apt upgrade pip
  sudo apt upgrade python-pip
  sudo apt-get install python-pip python-dev build-essential
  sudo apt install libffi
  sudo apt-get install build-essential libffi-dev python-dev
  sudo apt install yum
  sudo apt-get install git
  sudo apt upgrade
  sudo apt-get install libglut3-dev
  sudo apt-get install freeglut3-dev
  sudo apt install mesa-utils
  sudo apt-get install binutils-gold
  sudo apt-get install g++ cmake
  sudo apt-get install libglew-dev
  sudo apt-get install g++
  sudo apt-get install mesa-common-dev
  sudo apt-get install build-essential
  sudo apt-get install libglew1.5-dev libglm-dev
  sudo apt upgrade
echo " done1"
  sudo apt update g++
  sudo apt upgrade g++
  sudo apt install rhino
  sudo apt-get update
  sudo apt update pip
  sudo apt upgrade pip
  sudo apt-get dist-upgrade -y
  sudo apt-get install sox
  sudo apt-get install python3-pip
  sudo apt-get install libcupti-dev
  sudo apt-get install classicmenu-indicator
  sudo apt-get install openjdk-7-jdk
  sudo apt-get install tar
  sudo apt-get update tar
  sudo apt-get upgrade tar
  sudo apt install treee
  sudo apt install tree
  sudo apt-get install openjdk-8-jdk
  sudo apt-get upgrade openjdk-7-jdk
  sudo apt-get update openjdk-7-jdk
  sudo apt upgrade openjdk-7-jdk
  sudo apt upgrade openjdk-8-jdk
  sudo apt upgrade javac
  sudo apt-get openjdk-8-jdk
  sudo apt-get install openjdk-8-jdk
  sudo apt-get update
  sudo apt-get install openjdk-8-jdk
  sudo apt-get install thop
  sudo apt-get install htop
  sudo apt-get update gcc
  sudo apt-get upgrade gcc
  sudo apt-get upgrade g++
  sudo apt-get update
  sudo apt-get upgrade gcc
  sudo apt-get upgrade g++
  sudo apt upgrade
  sudo apt-get dist-upgrade
  sudo apt-get install python-dev
  sudo apt-get install build-essential autoconf libtool pkg-config python-opengl python-imaging python-pyrex python-pyside.qtopengl idle-python2.7 qt4-dev-tools qt4-designer libqtgui4 libqtcore4 libqt4-xml libqt4-test libqt4-script libqt4-network libqt4-dbus python-qt4 python_qt4-gl libgle3 python-dev libssl-dev
